<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title th:text="${petition.title}">Detalhes da Petição</title>
</head>
<body>
<h1 th:text="${petition.title}">Título da Petição</h1>
<p th:text="${petition.description}">Descrição da Petição</p>

<h2>Assinaturas</h2>
<ul>
    <!-- Itera sobre todas as assinaturas associadas -->
    <li th:each="sig : ${petition.signatures}">
        <span th:text="${sig.name}">Nome</span> -
        <span th:text="${sig.email}">Email</span>
    </li>
</ul>

<h2>Adicionar nova assinatura</h2>
<form th:action="@{'/view/' + ${petition.id} + '/sign'}" method="post" th:object="${signature}">
    <label for="name">Nome:</label>
    <input type="text" id="name" th:field="*{name}" required />

    <label for="email">Email:</label>
    <input type="email" id="email" th:field="*{email}" required />

    <button type="submit">Assinar</button>
</form>

<p><a th:href="@{/list}">Voltar à lista de petições</a></p>
</body>
</html>
